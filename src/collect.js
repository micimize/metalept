
export default function collect(unit){
  return `will collect all testable functions and related metadata
          from a given ${unit} (module, function, object)
          and return them for the assertion engine`
}
